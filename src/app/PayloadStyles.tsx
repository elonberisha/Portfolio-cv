'use client'

// Complete Payload CMS CSS — includes both UI components and admin layout/templates.
// @payloadcms/next/css = dist/prod/styles.css (306KB, all Payload styles bundled)
import '@payloadcms/next/css'

export default function PayloadStyles() {
  return null
}
